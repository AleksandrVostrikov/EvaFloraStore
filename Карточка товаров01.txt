<div class="card">
    <img src="@Url.Content("~/"+Model.ImageUrl)" alt="@Model.Name" class="card-img"/>
    <div class="card-body">
        <div class="row">
            <div class="col-8">
                <h4 class="card-title">@Model.Name</h4>
            </div>
            <div class="col-4">
                <span class="badge bg-dark" style="width:80px;">
                    <small>@Model.Price.ToString() руб.</small>
                </span>
            </div>
        </div>
        
        <p class="card-short">@Model.ShortDescription</p>
        <p class="card-description">@Html.Raw(Model.Description)</p>

        <a asp-action="AddToCart" 
        asp-controller="Cart" 
        asp-route-id="@Model.Id" 
        asp-route-returnUrl="@ViewContext.HttpContext.Request.PathAndQuery()"
           class="btn btn-dark btn-sm float-end mt-3" style="width:120px;">В корзину</a>
    </div>
</div>

.card {
    width: 400px;
    height: 400px;
    border-radius: 20px;
    overflow: hidden;
    border: 8px solid white;
}
.card:hover .card-body{
    right: 0;
}
.card-img{
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 15px;
}
.card-body {
    width: 100%;
    height: 100%;
    top: 0;
    right: 0;
    position: absolute;
    background: #5F9EA028;
    backdrop-filter: blur(5px);
    border-radius: 15px;
    color: #fff;
    padding: 30px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    right: -100%;
    transition: 0.5s;
}
.card-title {
    font-weight: 500;
}
.card-short{
    font-weight: 300;
}
.card-description {
    line-height: 25px;
    margin: 20px;
    font-weight: 400;
}
